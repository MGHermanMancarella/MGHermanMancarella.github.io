const links = document.querySelectorAll('a');
const colors = {
  green: '#042a2bff',
  moonstone: '#5eb1bfff',
  light: '#cdedf6ff',
  gold: '#e2c044ff',
  sinopia: '#cc3f0cff',
};

// for (let i = 0; i < links.length; i++) {
//   links[i].addEventListener('mouseover', function (event) {
//     event.target.style.color = colors.gold;
//   });
// }
